# docker-panel local config
APP_NAME=docker-panel
APP_ENV=dev

# Auth is disabled by default for local development in AuthController.
# Keep these values only for future auth-enabled mode.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=docker123
